// Alpymist: LibreWolf defaults, loaded after LibreWolf's own. Defaults only:
// anything changed in about:config wins.

// Open and save through the desktop's file dialog rather than LibreWolf's
// built-in GTK one, which ignores the dark theme; the portal's follows it.
pref("widget.use-xdg-desktop-portal.file-picker", 1);
