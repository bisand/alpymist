# The keyboard layout for Hyprland. The Alpymist installer replaces this with
# the layout chosen at install; edit it to change layouts, then hyprctl reload.
input {
    kb_layout = us
    kb_variant =
}
