# Alpymist: picom on the X11 desktop. Rounded corners and short fades, no blur
# or shadows: this tier runs on machines where every frame is CPU time.
corner-radius = 8;
rounded-corners-exclude = [ "class_g = 'Polybar'" ];
shadow = false;
fading = true;
fade-delta = 6;
vsync = true;
