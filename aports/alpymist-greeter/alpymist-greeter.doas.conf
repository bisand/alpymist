# The login screen's Restart and Power off. greetd's user may run these two
# commands as root, with no password, and nothing else.
permit nopass greetd as root cmd /sbin/reboot
permit nopass greetd as root cmd /sbin/poweroff
