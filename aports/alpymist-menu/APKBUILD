# Contributor: André Biseth <andre@biseth.net>
# Maintainer: André Biseth <andre@biseth.net>
pkgname=alpymist-menu
pkgver=0.0.1
pkgrel=6
pkgdesc="Alpymist menu: a searchable launcher and system menu on a Wayland layer surface"
url="https://github.com/bisand/alpymist"
arch="all"
license="MIT OR Apache-2.0"
# The window is wlr-layer-shell over wl_shm, drawn by Denise on the CPU: no
# GPU stack is needed, and the keymap is libxkbcommon's, which every Wayland
# compositor already pulls in. The fonts are runtime suggestions: without them
# the menu falls back to Denise's built-in bitmap and still reads correctly.
makedepends="cargo libxkbcommon-dev"
depends="font-fira-ttf font-nerd-fonts-symbols"
options="!check"  # the test suite runs in CI, not in the package build
source=""
builddir="$srcdir"

prepare() {
	# Same as alpymistctl: the workspace is bind-mounted at /src.
	tar -C /src -cf - \
		--exclude=./target \
		--exclude=./out \
		--exclude=./.git \
		. | tar -C "$builddir" -xf -
}

build() {
	cd "$builddir"
	cargo build --release --locked -p alpymist-menu
}

package() {
	install -Dm755 "$builddir"/target/release/alpymist-menu \
		"$pkgdir"/usr/bin/alpymist-menu
}
